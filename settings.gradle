pluginManagement {
  repositories {
    mavenCentral()
    gradlePluginPortal()
  }
}

dependencyResolutionManagement {
  versionCatalogs {
    libs {
      from(files('versions.toml'))
    }
  }
}

rootProject.name = 'gradle-buildinfra-plugin'