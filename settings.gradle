pluginManagement {
  repositories {
    mavenCentral()
    gradlePluginPortal()
  }
}

dependencyResolutionManagement {
  versionCatalogs {
  }
}

if (file("gradle-opts-plugin").exists()) {
  includeBuild('gradle-opts-plugin')
}

rootProject.name = 'gradle-buildinfra-plugin'